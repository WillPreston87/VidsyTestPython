# Having issues calling the locators from another Python file for now. Have moved these to the main test until this can be solved

'''from selenium import webdriver
driver = webdriver

class LoginPage:
    # text elements
    LOGO = By.className("avatar__thumbnail")
    # interactive elements
    EMAIL_FORM = ("email")
    PASSWORD_FORM = By.name("password")
    LOG_IN_BUTTON = By.className("login__button")

class BriefPage:
    # text elements
    BRAND_NAME = By.className("navigation__current-info")
    # interactive elements
    VIDSY_LOGO_BUTTON = By.className("avatar__thumbnail")
    ACCOUNT_BUTTON = By.partialLinkText("/settings")
    LOG_OUT_BUTTON = By.xpath("//button[contains(.,'Log out']")
    CREATE_BRIEF_BUTTON = By.className("sub-navigation__button ui-link is-interactive")
    CREATE_BRIEF_CARD = By.className("create-campaign-card")

class BrandSelectPage:
    CLOSE_BUTTON = By.className("brand-selector__close-button")
    BRAND_BUTTON = By.className("brand-card")'''
